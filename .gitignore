# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage

# typescript
*.tsbuildinfo

# environment — never commit secrets
.env
.env.*
!.env.example

# supabase local state
/supabase/.branches
/supabase/.temp
/supabase/.env

# os / editor
.DS_Store
*.pem
.vscode/
.idea/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# playwright
/test-results
/playwright-report
/blob-report
/playwright/.cache
.vercel
.env*
!.env.example

# Claude Code local settings and session state. Never intentionally tracked;
# a settings.json committed here becomes the permission default for every
# clone, which is a decision to make on purpose rather than by `git add -A`.
.claude/
